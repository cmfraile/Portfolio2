import { Injectable } from '@angular/core';
import { BehaviorSubject, Subject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class FondoeventoService {

  //window.addEventListener("resize",funcion);
  //obstamanio$ = new BehaviorSubject<number>(document.documentElement.clientWidth);
  
  constructor(){

}

  /*
  resizador(){
    let w:number = document.documentElement.clientWidth;
    console.log(w);
    this.holamundo$.next("Hola mundo");
    //this.holamundo$.next("Hola mundo:");
  }
  */

}
